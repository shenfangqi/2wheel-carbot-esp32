# Carbot PID Todo

## 当前状态

- 已完成编码器接入，`M1` 和 `M3` 的累计计数读取正常。
- 已确认 `M3` 编码器方向需要反号修正，当前软件里已经处理。
- 已完成最小双轮速度闭环，控制对象为 `M1` 和 `M3`。
- 当前 `diff_drive_forward()` / `diff_drive_backward()` 的输入语义已经变成目标 `rpm`，不再是开环 PWM。
- 当前 PID 遥测可输出 `target_rpm / actual_rpm / pwm_output`。

## 当前默认参数

- `kp = 0.8`
- `ki = 0.05`
- `kd = 0.0`

## 已确认结论

- 早期版本存在明显振荡，主要表现为 PWM 正反大幅翻转。
- 去掉 `D` 项并禁止“目标正转却输出反向 PWM”后，系统已明显稳定。
- 当前 `50 rpm` 阶跃测试下，`M1` 和 `M3` 都能稳定跟到目标附近。
- 当前低速反馈有明显量化台阶，属于编码器分辨率和低速死区共同作用的正常现象。

## 下一步 Todo

- 移除 `app_main` 里的自动 `pid step test`，恢复正常启动流程。
- 保留 PID 遥测日志，但把日志开关做成可控，而不是默认一直打印。
- 给 `kp / ki / kd` 增加运行时调参入口。
- 支持分别设置 `M1` 和 `M3` 的独立 PID 参数，不再共用一组。
- 明确并统一上层接口语义：`diff_drive_*()` 输入是 `rpm` 还是后续改成 `m/s`。
- 增加目标速度限幅，避免上层传入过大 `rpm`。
- 增加积分限幅和必要的抗积分饱和策略，避免长时间误差积累。
- 增加堵转、空转、无反馈等异常状态的保护与日志。
- 评估是否需要把 `motor_driver` 的死区补偿改成更平滑的起步策略。
- 在网页或 CLI 中增加 PID 参数查看与设置能力。
- 做固定速度长时间测试，记录稳态误差和左右轮一致性。
- 后续如接入里程计，再把轮速闭环输出接到整车运动学层。

## 建议顺序

1. 先移除自动阶跃测试，恢复正常控制流程。
2. 再做运行时调参入口。
3. 然后拆分左右轮 PID 参数。
4. 最后再考虑从 `rpm` 升级到 `m/s` 和里程计联动。
