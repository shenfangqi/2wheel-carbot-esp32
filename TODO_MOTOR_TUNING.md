# Carbot 电机控制 Todo

## 当前状态

- 当前项目使用 ESP-IDF `v5.4.4`。
- 当前车辆为阿克曼结构，只有两个驱动电机。
- 当前电机控制已经修正了 PWM 映射问题，速度相比之前已经提升。
- 当前 PWM 参数位于 `main/drivers/motor_driver.h`。
- 当前速度控制仍然直接使用 PWM tick 作为输入，不够直观，后续调参不方便。
- 当前设置了固定死区补偿 `PWM_MOTOR_DEAD_ZONE`，会影响低速细腻度。

## 已确认的问题

- 直接传入 PWM tick，不方便从控制层理解“速度”到底有多大。
- 死区补偿当前是固定叠加，低速时容易显得过猛。
- 速度输入、PWM 输出、死区补偿这三件事目前耦合在一起，后续维护和调参成本高。

## 下一步 Todo

- 外部输入改为 `0~100` 百分比，而不是直接传 PWM tick。
- 内部自动把百分比映射到 PWM 输出范围。
- 死区补偿只在起步阶段生效，不要在整个速度区间都固定叠加。
- 重新梳理 `motor_driver_set_speed()` 的输入语义，让上层调用更清晰。
- 重新测试低速起步、中速巡航、最高速表现，确认映射是否合理。
- 根据实车表现继续细调 `PWM_MOTOR_DEAD_ZONE`。

## 目标

- 让上层控制更容易理解和调试。
- 让小车低速更平顺，起步不突兀。
- 让电机速度控制逻辑更清晰，后续更容易维护。
