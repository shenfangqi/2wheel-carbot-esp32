# Carbot 直线跑偏与 IMU Todo

## 当前现象

- 小车在直线行驶时仍然存在走歪的情况。
- 倒车时偏移更明显，说明左右履带、传动阻力、接地摩擦或负载存在方向相关的不一致。
- 舵机中心偏置及 NVS 接口是旧底盘遗留能力，不参与当前履带底盘的航向控制。
- 左右驱动轮目前共用一组 PID 参数，还没有拆成左右独立参数。

## 当前判断

- 现阶段“走歪”不一定主要由左右轮 PID 引起。
- 从前面的 telemetry 看，左右轮实际转速大多数时候已经比较接近，没有明显长期单边失配。
- 倒车更容易跑偏，更像是左右履带有效半径、摩擦、张紧度、传动间隙和负载共同造成的问题。
- 如果轮子直径、摩擦、装配阻力不一致，软件可以补偿一部分，但不能完全替代机械一致性。

## 软件层面可做的三层补偿

1. 左右轮速度闭环
- 先保证左右轮尽量跟上目标速度。
- 当前已经有双轮速度 PID，但还没拆左右独立参数。

2. 固定补偿
- 通过左右履带目标速度固定偏置，补偿“总是往同一边偏”的问题。
- 当前差速控制层已经提供直行右履带固定修正；舵机偏置不能用于履带底盘校正。

3. IMU 外环修正
- 使用 `gyro_z` 检测车身是否持续偏航。
- 当目标是直线时，期望 `yaw_rate = 0`。
- 如果 `gyro_z` 持续偏正或偏负，则动态修正左右履带速度差。

## 关于 IMU 的结论

- IMU 可以在软件层面缓解轮子不一致、地面扰动、轻微机械偏差带来的直线跑偏。
- IMU 不能完全解决严重机械问题，只能做动态补偿。
- 上 IMU 外环之前，履带张紧度、有效半径、摩擦和左右轮基本速度一致性仍然应该先压到可接受范围。

## 关于左右轮独立 PID 的结论

- 上 IMU 外环，并不强制要求先完成左右轮独立 PID。
- 但如果左右轮本身特性差异明显，独立 PID 会让 IMU 外环更稳、更容易调。
- 当前阶段可以先保留共用 PID，先验证 IMU 外环是否能改善直线表现。
- 如果后续发现外环长期补偿过大，再拆分左右轮独立 PID。

## 建议方案

### 第一阶段

- 保持当前轮速 PID 不动。
- 校准左右履带的机械状态与固定目标速度修正，不使用遗留舵机偏置。
- 重点确认前进直线时的稳定性，不优先处理倒车直线。

### 第二阶段

- 做一个最小 IMU 外环版本。
- 只在“前进直线”时启用。
- 使用 `gyro_z` 作为偏航误差输入。
- 将航向误差转换为左右履带速度差。

原因：

- 当前车辆是履带式差速结构，偏航由左右履带速度差产生。
- 舵机不是当前底盘的运动学转向源，IMU 外环必须作用于差速目标。

### 第三阶段

- 如果 IMU 外环已经接入，但补偿量长期偏大：
- 再拆分 `M1` / `M3` 独立 PID 参数。
- 必要时再增加左右轮目标速度固定偏置。

## 暂不在本次实现中处理

- 左右轮独立 PID 参数拆分
- IMU 外环直线保持
- 前进/倒车分开的左右履带补偿策略
- 更复杂的航向保持控制

## 当前优先级

- 低优先级
- 本次只记录方案，不进入实现
